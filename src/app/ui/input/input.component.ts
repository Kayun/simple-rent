import { Component, Input, OnInit, HostBinding } from '@angular/core';

@Component({
  selector: 'sr-input',
  template: require('./input.component.pug'),
  styles: [ require('./input.component.styl') ]
})
export class InputComponent {
}

import { Component, Input } from '@angular/core';

@Component({
  selector: 'sr-radio',
  template: require('./radio.component.pug'),
  styles: [ require('./radio.component.styl') ]
})
export class RadioComponent {
}

@import "~helpers/index"

:host
  display: block
  h-align()
  top: 20%
  width: 1024px
  padding: $rhythm(1)

.title
  adjust-font-size-to(34px, 2)
  font-weight: 300
  margin-bottom: $rhythm(2)
  text-align: center

.search
  width: 100%

  &-input
    position: relative
    z-index: 1
    width: 75%

  &-submit
    position: relative
    z-index: 2
    width: 25%

sr-main-panel
  margin-top: $rhythm(1)
